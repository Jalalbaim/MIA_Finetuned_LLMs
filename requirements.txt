# Local development environment (Windows, .venv). Reproduces the workshop
# pipeline end to end, including the parts E1 does not use (opacus, peft-from-git).
#
# DO NOT install this on Kaggle. The numpy pin below downgrades the base image
# underneath a pandas compiled against numpy 2.x headers, and every subsequent
# `import pandas` -- so also datasets, transformers, sklearn -- fails with
#     ValueError: numpy.dtype size changed ... Expected 96 from C header, got 88
# The Kaggle image already satisfies everything E1 imports; run
# E1_scaled_xps/kaggle/preflight.py to verify a session instead of installing.
torch==2.5.1                  # must be >=2.4 for torch.library.register_fake (torchvision, pulled in transitively by transformers, requires it)
torchvision==0.20.1           # matched pair for torch 2.5.1; not used directly, but transformers' lazy imports (e.g. BloomPreTrainedModel via peft.utils.constants) load it
transformers==4.46.3          # GPT-Neo 125M weights + tokenizer; must be >=4.44 for EncoderDecoderCache (peft 0.19.1 import-time dependency)
datasets==2.20.0              # Enron email download + HF hub access
accelerate==0.34.0
peft @ git+https://github.com/huggingface/peft.git@1598ecb8fc504bfcb08b9b232b295414a729d7ed
                               # LoRA + MiCA adapters. MiCA (init_lora_weights="mica") is
                               # unreleased -- not in peft 0.19.1 on PyPI, only on `main` as
                               # of the pinned commit above. Re-pin to a newer commit (or
                               # switch to a PyPI pin) once peft cuts a release containing it.
 
opacus==1.5.2                 # DP-SGD with RDP accounting; requires torch>=2.0
 
numpy==1.26.4               
scipy==1.13.1            
scikit-learn==1.5.0    

zstandard==0.22.0          
pandas==2.2.2          

matplotlib==3.9.0           
seaborn==0.13.2              
 
tqdm==4.66.4
jsonlines==4.0.0
pyyaml==6.0.1

huggingface_hub==0.36.2       # E1 checkpoint/cache sink; Kaggle sessions die at 12h so
                              # runs/*/training_state.pt and cache/*.npz are pushed to a
                              # private Hub dataset repo. Pinned explicitly rather than
                              # relying on the transitive pull from transformers/datasets.
pytest==9.0.3                 # E1_scaled_xps/tests/test_e1.py -- the Week 1 harness gate

nbformat==5.10.4              # build/read figures/figure_plotting.ipynb
nbclient==0.10.0              # headless execution of figure_plotting.ipynb
ipykernel==6.29.5              # Python kernel for notebook execution
 